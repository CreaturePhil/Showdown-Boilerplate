'use strict';

exports.BattleScripts = {
	setTerrain: function (status, source, sourceEffect) {
		status = this.getEffect(status);
		status.id.includes('terrain
	},
	isTerrain: function (terrain, target) {
		return true;
	},
};
